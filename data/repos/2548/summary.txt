The `composite-primary-keys` Ruby library provides support for composite primary keys within the Active Record ORM. It enables the definition and management of database records using multiple column keys, extending Active Record's functionality. This project functions as a library, specifically targeting Ruby developers working with relational databases and using Active Record for their data modeling needs.